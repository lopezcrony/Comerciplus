name: comerciplus
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  camera: 0.10.0
  fl_chart: ^0.69.0
  flutter:
    sdk: flutter
  flutter_secure_storage: ^5.0.0
  flutter_dotenv: 5.2.1
  path_provider: ^2.0.13
  flutter_plugin_android_lifecycle: ^2.0.10
  flutter_svg: ^2.0.14
  google_fonts: ^6.2.1
  shared_preferences: ^2.1.0
  logging: ^1.2.0
  http: ^1.2.2
  intl: ^0.19.0
  url_launcher: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  flutter_animate: ^4.5.0
  mobile_scanner: ^3.5.5
  flutter_launcher_icons: "^0.9.2"

flutter:
  uses-material-design: true
  assets:
    - assets/.env
    - assets/images/iconoc.png
    - assets/images/logo-simbolo.JPG
    - assets/images/logo-name.JPG
    - assets/images/logo-completo.jpeg

flutter_icons:
  android: true
  ios: true
  image_path: "assets/images/logo-simbolo.JPG"  # Aquí pones la ruta de tu imagen