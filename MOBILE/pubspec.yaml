name: comerciplus
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  camera: 0.10.0
  fl_chart: ^0.69.0
  flutter:
    sdk: flutter
  flutter_dotenv: 5.2.1
  flutter_svg: ^2.0.14
  http: ^1.2.2
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  flutter_animate: ^4.5.0

flutter:
  uses-material-design: true
  assets:
    - assets/.env